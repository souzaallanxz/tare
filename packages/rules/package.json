{
  "name": "@tare/rules",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint: rules'",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@tare/core": "workspace:*"
  },
  "devDependencies": {
    "@tare/config": "workspace:*",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "rimraf": "^6.0.1"
  }
}
